# ============================================
# frps (服务端) 参考配置 — 部署在 VPS 上
# 运行: frps.exe -c frps.toml
# ============================================

bindAddr = "0.0.0.0"
bindPort = 7000

# 认证 Token（与 frpc 端保持一致，已自动生成强随机值）
auth.token = "your_shared_token_here"

# 允许的端口范围
allowPorts = [
  { start = 3000, end = 3000 },
  { start = 8000, end = 9000 }
]

# 仪表盘（仅本机访问，已自动生成强密码）
webServer.addr = "127.0.0.1"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "your_dashboard_password_here"

# 日志
log.to = "./frps.log"
log.level = "info"
log.maxDays = 7